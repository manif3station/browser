# EPIC-008

## Title

Add screenshot output to the `browser` skill.

## Outcome

Add a dedicated `browser.png` command so DD users can capture a rendered page screenshot directly from the browser skill and get back the saved PNG path.

## Tickets

- `DD-031` Add `browser.png` screenshot capture with file-path output
