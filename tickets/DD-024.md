# DD-024

## Title

Add controller-mode browser journeys to the `browser` skill.

## Method

TDD

## Acceptance

- `browser.get` accepts `--playwright`, `--agent`, and `--flow`
- those flags switch `--script` into a Perl Playwright controller script
- controller scripts can continue through multi-page browser journeys from the starting URL
- `--ask` remains compatible so manual login or CAPTCHA work can happen before the scripted flow continues

## Status

Done
