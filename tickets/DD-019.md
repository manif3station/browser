# DD-019

## Title

Create the `browser` project records and dependency files.

## Method

BDD

## Acceptance

- `skills/browser` exists as an isolated git repository
- the skill has `README.md`, `.env`, `Changes`, `docs/`, and `tickets/`
- the skill declares `aptfile`, `brewfile`, and `cpanfile`

## Status

Done
