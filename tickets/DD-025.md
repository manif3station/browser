# DD-025

## Title

Keep `browser` ask-mode sessions open on slow login pages.

## Method

TDD

## Acceptance

- ask-mode initial navigation does not fail just because the page never reaches `networkidle`
- ask-mode initial navigation stays open by default for manual login or CAPTCHA work
- `--timeout-ms` still works as an explicit override

## Status

Done
