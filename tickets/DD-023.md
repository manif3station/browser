# DD-023

## Title

Add interactive ask-mode to the `browser` skill.

## Method

TDD

## Acceptance

- `browser.get` accepts `--ask` and `--askme`
- ask-mode forces a visible browser session instead of headless mode
- the user can complete captcha or login interaction and then continue payload capture

## Status

Done
